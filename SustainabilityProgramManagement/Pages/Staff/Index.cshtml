@page
@model SustainabilityProgramManagement.Pages.Staff.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StaffMember[0].FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StaffMember[0].LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StaffMember[0].SustainabilityProgram)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.StaffMember) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SustainabilityProgram.ProgramName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.StaffMemberId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.StaffMemberId">Details</a> |
                <a asp-page="./Projects" asp-route-staffid="@item.StaffMemberId">Project Report</a> |
                <a asp-page="./Delete" asp-route-id="@item.StaffMemberId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
